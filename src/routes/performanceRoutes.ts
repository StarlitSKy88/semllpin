import { Router } from 'express';

const router = Router();

// Performance routes temporarily disabled due to compilation errors
// Will be re-enabled after fixing the underlying services

export default router;